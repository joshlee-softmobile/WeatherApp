# API endpoint used by the frontend (relative path for local Vite server proxy)
VITE_API_ENDPOINT=/api/weather

# Target server for the Vite proxy (your local Cloudflare Worker)
VITE_API_PROXY_TARGET=http://127.0.0.1:8787
